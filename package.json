{
  "repository": {
    "type": "git",
    "url": "https://github.com/Zhuul/seamless-ai-bridge.git"
  },
  "name": "seamless-ai-bridge",
  "publisher": "zhuul",
  "displayName": "Seamless AI Bridge",
  "description": "An extensible chat participant for VS Code designed to route prompts to various AI models.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.108.1"
  },
  "activationEvents": [
    "*",
    "onChatParticipant:seamless-ai-bridge"
  ],
  "categories": [
    "Other"
  ],
  "main": "./extension.js",
  "contributes": {
    "chatParticipants": [
      {
        "id": "seamless-ai-bridge",
        "name": "bridge",
        "fullName": "Seamless AI Bridge",
        "description": "Bridge for connecting custom and self-hosted AI models.",
        "isSticky": true
      }
    ],
    "configuration": {
      "title": "Seamless AI Bridge",
      "properties": {
        "seamlessAiBridge.path": {
          "type": "string",
          "markdownDescription": "Absolute path to an external bridge CLI. If empty, the extension uses the bundled `bridge-echo.js`. You can also set the environment variables `SEAMLESS_AI_BRIDGE_PATH` or `SEAMLESS_AI_CLI_PATH`.",
          "default": ""
        },
        "seamlessAiBridge.experimental.enableCrossParticipantChat": {
          "type": "boolean",
          "markdownDescription": "Experimental and unstable. When enabled, @bridge may attempt best-effort cross-participant command invocation before automatically falling back to the local bridge provider.",
          "default": false
        },
        "seamlessAiBridge.personas": {
          "type": "array",
          "markdownDescription": "Configurable personas for @bridge. Each persona can be addressed as `@<alias>` and uses its own Copilot model selector and conversation history.",
          "default": [],
          "items": {
            "type": "object",
            "required": [
              "alias"
            ],
            "properties": {
              "alias": {
                "type": "string",
                "description": "Persona alias used in chat, for example `planner` for `@planner`."
              },
              "name": {
                "type": "string",
                "description": "Optional display name for the persona."
              },
              "enabled": {
                "type": "boolean",
                "description": "Enable or disable this persona.",
                "default": true
              },
              "provider": {
                "type": "string",
                "description": "Reserved for future multi-provider support. V4 supports `copilot` only.",
                "default": "copilot"
              },
              "modelSelector": {
                "type": "object",
                "description": "Copilot model selector fields used with vscode.lm.selectChatModels.",
                "properties": {
                  "vendor": {
                    "type": "string",
                    "description": "Model vendor. V4 enforces `copilot`."
                  },
                  "id": {
                    "type": "string",
                    "description": "Specific model ID if known."
                  },
                  "family": {
                    "type": "string",
                    "description": "Model family, for example `gpt-4o`."
                  },
                  "version": {
                    "type": "string",
                    "description": "Optional model version."
                  }
                }
              }
            }
          }
        },
        "seamless-ai-bridge.debug.enabled": {
          "type": "boolean",
          "description": "Enable detailed debug logging to the Seamless AI Bridge output channel.",
          "default": false
        }
      }
    }
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.108.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "eslint": "^9.39.2",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
